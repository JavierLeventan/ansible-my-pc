# Reflector configuration file for the systemd service.
#
# Empty lines and lines beginning with "#" are ignored.  All other lines should
# contain valid reflector command-line arguments. The lines are parsed with
# Python's shlex modules so standard shell syntax should work. All arguments are
# collected into a single argument list.
#
# See "reflector --help" for details.

--save {{ arch_config_reflector_save }}
--country {{ arch_config_reflector_countries }}
--age {{ arch_config_reflector_age }}
--protocol {{ arch_config_reflector_protocol }}
--sort {{ arch_config_reflector_sort }}
--threads {{ arch_config_reflector_threads }}